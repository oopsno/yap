language: python
# ensure GLIBCXX_3.4.19
before_install:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq
  - sudo apt-get install -qq gcc-5
python:
  - "2.7"
  - "3.5"
  - "3.6"
cache: pip
install:
  - pip install -r requirements-cpu.txt
script: pytest test